1. What is the difference between var, let, and const?

The differences between var, let and const :
--> var is a old version and it is redeclarable. 
--> let is reassignable and latest version.
--> const is not reassignable.

2. What is the spread operator (...)?

spread operator spreads array and object

3. What is the difference between map(), filter(), and forEach()?

The differences between:
--> map() create a new array. 
--> filter() create a new array based on condition
--> forEach() its looping on every elements

4. What is an arrow function?
--> An arrow function is ES6 version of javascript. It is a shorter version.

5. What are template literals?
--> Template literals is ES6 version of javascript. It more strong and dinamic than double and single quotes.
